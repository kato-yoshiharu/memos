# Yazi

Rust製のターミナルファイラー。

## 基本操作

- `yazi`: 起動する

## 検索

現在のディレクトリ内を絞り込む。

- `f`: フィルタ。入力に応じて一覧が絞られる
- `/`: 次を検索、`?`: 前を検索
- `n`: 次の一致へ、`N`: 前の一致へ

配下を再帰的に探す。

- `s`: fd を使って名前で検索する
- `S`: ripgrep を使って中身で検索する
- `Ctrl+S`: 実行中の検索を中止する

別の場所へ飛ぶ。

- `z`: fzf でディレクトリを選んで移動する
- `Z`: zoxide の履歴から移動する

## パスのコピー

`c` がコピー用のプレフィックス。

- `c` → `c`: 絶対パス
- `c` → `d`: ディレクトリのパス
- `c` → `f`: ファイル名

`Space` で複数選択した状態なら、改行区切りでまとめてコピーされる。

## 参考

- <https://zenn.dev/mozumasu/scraps/211222cfc68b75>

## TODO

- smart-paste.yazi
